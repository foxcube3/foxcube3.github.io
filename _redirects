# Removed self-referential redirects that were causing loops.
# If you need to redirect from an old domain to fxcb3.work.gd on Netlify,
# use domain-specific rules like:
# https://old.example.com/* https://fxcb3.work.gd/:splat 301!